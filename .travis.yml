sudo: required
dist: trusty

language: cpp

addons:
  apt:
    packages:
      - build-essential
      - cmake
      - g++-8
      - gcc-8
      - git
      - libudev-dev
      - libevdev-dev
      - libyaml-cpp-dev

script:
  - ./build
